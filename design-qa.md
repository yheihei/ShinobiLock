# デザイン改修の検証

- source visual truth: 提供された `Shinobi Lock.html` の2a〜2q
- 参照のビューポート: 402 × 874、375 × 667
- HTMLの構造、文言、色、サイズ指定は確認済み。
- 元デザインの画像取得: blocked。アプリ内ブラウザと、ユーザーが開いたChromeの既存タブの両方で、`file://` がブラウザのURLポリシーにより拒否された。別経路でのブラウザ取得は行っていない。

## 実装とビルド

- 背景、カード、アクセント、文字階層、角丸、ボタン寸法をHTMLの指定から反映。
- InterのLatinフォントをHTMLから抽出し、RegularとMediumをアプリに同梱。日本語はiOSのフォントへフォールバックする。
- 一般アイコンとスイッチはiOSのSF Symbols・Toggle。アプリ名とアイコンはFamily Controlsの表示を維持。
- 曜日の一括選択、カードから編集、選択アプリの常時表示、項目別エラー、広告の状態別メッセージ、解除の進捗バー、削除・休止確認を反映。
- 広告SDKの扱う情報は設定の開閉できる説明に残した。広告の通信情報まで端末内保存と誤解させないための調整。
- iOS Simulator向けビルド成功。
- iOS実機向けの署名付きビルド成功。接続したiPhone 12 mini / iOS 26.6.2へインストールし、起動成功。
- `swift test`: XCTest 3件 + Swift Testing 14件、すべて成功。
- `git diff --check`、Info.plist、project.pbxprojの構文確認に成功。
- AppIntentsを利用していないことによるメタデータ抽出スキップの警告のみ。

## 表示と操作

画面確認用コピーは `build/redesign-preview`。本体のViewを使い、権限・アプリ名・広告結果をサンプルに置き換える。配布する本体にはサンプル状態やバイパスを追加していない。

元デザインを画像として取得できていないため、同じ寸法の画像を並べた比較と、文字・余白のピクセル単位の一致確認は未実施。HTMLの値が一致することをもって、画像比較に合格したとは扱わない。

## 実機で確認した表示

- implementation screenshot: `output/redesign/01-home-iphone12mini.png`。実機の個人設定を含むためローカルで保管。
- 画面: ホーム、ルール1件、休止中、現在のロック0件。
- デバイス: iPhone 12 mini / iOS 26.6.2。
- ビューポート: 375 × 812 pt。画像: 1125 × 2436 px、3倍密度。
- ホームのタイトル、状態カード、ルール名、時刻、曜日と件数、休止バッジ、作成ボタンを実画像で確認。横にはみ出す表示や切れた文字は見当たらない。
- 更新前後のルールID・名前・曜日・時刻・有効状態・アプリの選択トークンを比較し、一致。集合の順序を正規化して比較した。休止状態は維持され、ロック0件。
- 元デザインの2cとはルール数とロック状態が異なるため、同じ状態の画像比較には使っていない。
- 通常サイズ・小型シミュレータはOS起動とアプリ起動要求で停止した。シミュレータの表示成功は検証結果に含めない。

## 利用者の確認

2026年9月13日、実機への更新後に利用者から「大丈夫でした」と確認を受け、pushの指示を受けた。画面ごとの確認範囲は記録していない。

## エージェントによる未確認項目

- 元デザインと実装の全体画像・部分画像の比較。
- 作成・編集・設定・解除の実機表示と操作。iPhoneミラーリングが未設定のため、エージェントによる操作と撮影は未実施。
- 実機でのFamilyActivityPicker、スクリーンタイム許可、Shield、広告配信と再ロック。

final result: blocked。元デザインの画像取得ができず、画像比較の検証は未完了。利用者による更新版の確認とpushの承認は取得済み。
